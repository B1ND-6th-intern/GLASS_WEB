import "./LoginBar.css";

const LoginBar = () => {
  return (
    <section id="Loginbar">
      <div className="loginbar-container">
        <div className="loginbar-title">로그인</div>
      </div>
    </section>
  );
};

export default LoginBar;
